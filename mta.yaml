_schema-version: "3.2"
ID: sapmbem
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: sapmbem-destination-content
  type: com.sap.application.content
  requires:
  - name: sapmbem-destination-service
    parameters:
      content-target: true
  - name: sapmbem-repo-host
    parameters:
      service-key:
        name: sapmbem-repo-host-key
  - name: sapmbem-uaa
    parameters:
      service-key:
        name: sapmbem-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: sapmbem_html_repo_host
          ServiceInstanceName: sapmbem-html5-srv
          ServiceKeyName: sapmbem-repo-host-key
          sap.cloud.service: sapmbem
        - Authentication: OAuth2UserTokenExchange
          Name: sapmbem_uaa
          ServiceInstanceName: sapmbem-xsuaa-srv
          ServiceKeyName: sapmbem-uaa-key
          sap.cloud.service: sapmbem
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: sapmbem-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: sapmbem-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sapmbem.zip
      name: sapmbem
      target-path: resources/
- name: sapmbem
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: sapmbem-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: sapmbem-destination-service
    service-plan: lite
- name: sapmbem-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: sapmbem-xsuaa-srv
    service-plan: application
- name: sapmbem-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: sapmbem-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
